Public Module Module1

public struct Alumno
     Dim edad As Integer
    end struct

    Sub Main()
	if x = x then
            		Dim mynumber = 3
     	else if x = 2 then
             		dim x = 0
        	end if
  	for x = 5 to 10 step 1
        		console.writeLine("Llamando Funcion Suma")
	next x
	Do until cont = 3
        		console.writeLine("Edad del alumno: " & estudiante.edad)
	loop
    Dim result As Integer
    result = suma(3,2)
    console.writeLine("Resultado es igual: " & result)
    End Sub
End Module