Public Module Module1

    Sub Main()
    for x = 5 to 10 step 1
    Do until cont = 3
        console.writeLine("Edad del alumno: " & estudiante.edad)
	if x = x then
       	 Dim mynumber = 3
 	else if x = 2 then
         	dim x = 0
        end if
    loop
    next x
End Sub
End Module